import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.siridhanya.hub',
  appName: 'Siri-Dhanya Smart Agri Hub',
  webDir: 'dist'
};

export default config;
